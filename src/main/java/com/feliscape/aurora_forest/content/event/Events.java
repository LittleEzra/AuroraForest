package com.feliscape.aurora_forest.content.event;

import com.feliscape.aurora_forest.Aurora;
import com.feliscape.aurora_forest.util.ColorGradient;
import com.feliscape.aurora_forest.util.ColorUtil;
import net.neoforged.fml.common.EventBusSubscriber;
import net.neoforged.neoforge.client.event.RegisterColorHandlersEvent;

public class Events {
    //@EventBusSubscriber(modid = Aurora.MOD_ID, bus = EventBusSubscriber.Bus.GAME)
    private static class GameEvents{

    }
    //@EventBusSubscriber(modid = Aurora.MOD_ID, bus = EventBusSubscriber.Bus.MOD)
    private static class ModBusEvents{

    }
}
